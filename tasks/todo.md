# Superadmin Deletion Plan

- [x] **1. Task Management / Plan Checklist**
  - [x] Write this checklist to `tasks/todo.md`.
  - [x] Verify plan through explicit approval.

- [x] **2. Database Cleanup (User Merge)**
  - [x] Create `backend/delete-superadmin.js` to:
    - [x] Find the internal `id` of both `superadmin` and `admin1`.
    - [x] Update any `user_id` foreign keys (like the `sales` table) on `superadmin`'s records to belong to `admin1`.
    - [x] Execute `DELETE FROM users WHERE username = 'superadmin';`.
  - [x] Run the migration script.

- [x] **3. Codebase Audit (Hardcoded Strings)**
  - [x] Update `frontend/src/pages/Users.jsx` to change the hardcoded deletion safety check from `u.username === 'superadmin'` to `u.username === 'admin1'`.

- [x] **4. Backend Validation**
  - [x] Delete the `superadmin` record from the seed array in `backend/setup-db.js`.
  - [x] Update associated default credentials console.logs.

## Review Section
The `superadmin` account has been completely purged from the application stack. All pre-existing user activity logs generated by `superadmin` were gracefully handed off to `admin1` natively in the database level. The deletion-safety flags on the Users frontend render are now aimed successfully at `admin1`.
